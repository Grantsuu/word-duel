import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import WordDuel from './pages/word-duel';

ReactDOM.render(
  <React.StrictMode>
    <WordDuel />
  </React.StrictMode>,
  document.getElementById('root')
);
